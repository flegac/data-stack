[project]
name = "measure-io-influxdb"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "flegac", email = "florent.legac@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "influxdb-client>=1.48.0",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
